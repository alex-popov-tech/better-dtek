# Redis (Vercel KV / Upstash / Redis Labs)
# Get from Vercel project settings > Storage
REDIS_URL=

# Sentry Error Tracking
# Get DSN from: sentry.io → Project Settings → Client Keys
PUBLIC_SENTRY_DSN=

# Environment tag (development, staging, production)
PUBLIC_SENTRY_ENVIRONMENT=development

# Source Maps Upload (optional for local, required for Vercel)
# Get token from: sentry.io → Settings → Auth Tokens
# Scopes needed: project:releases, org:read
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
